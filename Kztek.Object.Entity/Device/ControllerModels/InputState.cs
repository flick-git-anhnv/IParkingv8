namespace Kztek.Object.Entity.Device.ControllerModels
{
    public enum InputState
    {
        Open = 0, // 0 = open ( = true)
        Close = 1, // 1 = close ( = false)
        Disconnected = 2, // 2 = disconnected
        Error = 3// 3 = error
    }
}
